import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    margin: 16,
    borderWidth: 1,
    padding: 10,
    borderColor: 'white',
    borderRadius: 8,
    color: 'white',
  },
});

export default styles;
